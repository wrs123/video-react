export enum DownloadStatus {
    PENDING,
    ERROR,
    FINISH,
    PAUSE,
    ANAL //解析中
}

export enum ResultStatus {
    ERROR = "ERROR",
    OK = "OK"
}

export enum DownloadFileType{
    MP4 = "MP4",
    M3U8 = "M3U8",
}
